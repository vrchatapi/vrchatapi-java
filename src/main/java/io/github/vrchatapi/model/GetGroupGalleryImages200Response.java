/*
 * VRChat API Documentation
 *
 * The version of the OpenAPI document: 1.21.0
 * Contact: vrchatapi.lpv0t@aries.fyi
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package io.github.vrchatapi.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.github.vrchatapi.model.GroupGalleryImage;
import io.github.vrchatapi.model.GroupGalleryImageList;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import io.github.vrchatapi.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.24.0")
public class GetGroupGalleryImages200Response extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(GetGroupGalleryImages200Response.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!GetGroupGalleryImages200Response.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'GetGroupGalleryImages200Response' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);

            final Type typeInstanceListGroupGalleryImage = new TypeToken<List<GroupGalleryImage>>(){}.getType();
            final TypeAdapter<List<GroupGalleryImage>> adapterListGroupGalleryImage = (TypeAdapter<List<GroupGalleryImage>>) gson.getDelegateAdapter(this, TypeToken.get(typeInstanceListGroupGalleryImage));
            final TypeAdapter<GroupGalleryImageList> adapterGroupGalleryImageList = gson.getDelegateAdapter(this, TypeToken.get(GroupGalleryImageList.class));

            return (TypeAdapter<T>) new TypeAdapter<GetGroupGalleryImages200Response>() {
                @Override
                public void write(JsonWriter out, GetGroupGalleryImages200Response value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `List<GroupGalleryImage>`
                    if (value.getActualInstance() instanceof List<?>) {
                        List<?> list = (List<?>) value.getActualInstance();
                        if (!list.isEmpty() && list.get(0) instanceof GroupGalleryImage) {
                            JsonArray array = adapterListGroupGalleryImage.toJsonTree((List<GroupGalleryImage>)value.getActualInstance()).getAsJsonArray();
                            elementAdapter.write(out, array);
                            return;
                        }
                    }
                    // check if the actual instance is of the type `GroupGalleryImageList`
                    if (value.getActualInstance() instanceof GroupGalleryImageList) {
                        JsonElement element = adapterGroupGalleryImageList.toJsonTree((GroupGalleryImageList)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: GroupGalleryImageList, List<GroupGalleryImage>");
                }

                @Override
                public GetGroupGalleryImages200Response read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize List<GroupGalleryImage>
                    try {
                        // validate the JSON object to see if any exception is thrown
                        if (!jsonElement.isJsonArray()) {
                            throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected json element to be a array type in the JSON string but got `%s`", jsonElement.toString()));
                        }

                        JsonArray array = jsonElement.getAsJsonArray();
                        // validate array items
                        for(JsonElement element : array) {
                            GroupGalleryImage.validateJsonElement(element);
                        }
                        actualAdapter = adapterListGroupGalleryImage;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'List<GroupGalleryImage>'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for List<GroupGalleryImage> failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'List<GroupGalleryImage>'", e);
                    }
                    // deserialize GroupGalleryImageList
                    try {
                        // validate the JSON object to see if any exception is thrown
                        GroupGalleryImageList.validateJsonElement(jsonElement);
                        actualAdapter = adapterGroupGalleryImageList;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'GroupGalleryImageList'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for GroupGalleryImageList failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'GroupGalleryImageList'", e);
                    }

                    if (match == 1) {
                        GetGroupGalleryImages200Response ret = new GetGroupGalleryImages200Response();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format(java.util.Locale.ROOT, "Failed deserialization for GetGroupGalleryImages200Response: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public GetGroupGalleryImages200Response() {
        super("oneOf", Boolean.FALSE);
    }

    public GetGroupGalleryImages200Response(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("List<GroupGalleryImage>", List.class);
        schemas.put("GroupGalleryImageList", GroupGalleryImageList.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return GetGroupGalleryImages200Response.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * GroupGalleryImageList, List&lt;GroupGalleryImage&gt;
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof List<?>) {
            List<?> list = (List<?>) instance;
            if (!list.isEmpty() && list.get(0) instanceof GroupGalleryImage) {
                super.setActualInstance(instance);
                return;
            }
        }

        if (instance instanceof GroupGalleryImageList) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be GroupGalleryImageList, List<GroupGalleryImage>");
    }

    /**
     * Get the actual instance, which can be the following:
     * GroupGalleryImageList, List&lt;GroupGalleryImage&gt;
     *
     * @return The actual instance (GroupGalleryImageList, List&lt;GroupGalleryImage&gt;)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `List&lt;GroupGalleryImage&gt;`. If the actual instance is not `List&lt;GroupGalleryImage&gt;`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `List&lt;GroupGalleryImage&gt;`
     * @throws ClassCastException if the instance is not `List&lt;GroupGalleryImage&gt;`
     */
    @SuppressWarnings("unchecked")
    public List<GroupGalleryImage> getListGroupGalleryImage() throws ClassCastException {
        return (List<GroupGalleryImage>)super.getActualInstance();
    }

    /**
     * Get the actual instance of `GroupGalleryImageList`. If the actual instance is not `GroupGalleryImageList`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `GroupGalleryImageList`
     * @throws ClassCastException if the instance is not `GroupGalleryImageList`
     */
    @SuppressWarnings("unchecked")
    public GroupGalleryImageList getGroupGalleryImageList() throws ClassCastException {
        return (GroupGalleryImageList)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to GetGroupGalleryImages200Response
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with List<GroupGalleryImage>
        try {
            if (!jsonElement.isJsonArray()) {
                throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected json element to be a array type in the JSON string but got `%s`", jsonElement.toString()));
            }
            JsonArray array = jsonElement.getAsJsonArray();
            // validate array items
            for(JsonElement element : array) {
                GroupGalleryImage.validateJsonElement(element);
            }
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for List<GroupGalleryImage> failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with GroupGalleryImageList
        try {
            GroupGalleryImageList.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for GroupGalleryImageList failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format(java.util.Locale.ROOT, "The JSON string is invalid for GetGroupGalleryImages200Response with oneOf schemas: GroupGalleryImageList, List<GroupGalleryImage>. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of GetGroupGalleryImages200Response given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of GetGroupGalleryImages200Response
     * @throws IOException if the JSON string is invalid with respect to GetGroupGalleryImages200Response
     */
    public static GetGroupGalleryImages200Response fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, GetGroupGalleryImages200Response.class);
    }

    /**
     * Convert an instance of GetGroupGalleryImages200Response to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

