/*
 * VRChat API Documentation
 *
 * The version of the OpenAPI document: 1.21.0
 * Contact: vrchatapi.lpv0t@aries.fyi
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package io.github.vrchatapi.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.github.vrchatapi.model.FavoriteGroupSummary;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import io.github.vrchatapi.JSON;

/**
 * A user&#39;s favorite groups of one type, with the limits that apply to them.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.24.0")
public class FavoriteGroupList {
  public static final String SERIALIZED_NAME_FAVORITE_GROUPS = "favoriteGroups";
  @SerializedName(SERIALIZED_NAME_FAVORITE_GROUPS)
  @javax.annotation.Nonnull
  private List<FavoriteGroupSummary> favoriteGroups = new ArrayList<>();

  public static final String SERIALIZED_NAME_MAX_FAVORITE_GROUPS = "maxFavoriteGroups";
  @SerializedName(SERIALIZED_NAME_MAX_FAVORITE_GROUPS)
  @javax.annotation.Nullable
  private Integer maxFavoriteGroups;

  public static final String SERIALIZED_NAME_MAX_FAVORITES_PER_GROUP = "maxFavoritesPerGroup";
  @SerializedName(SERIALIZED_NAME_MAX_FAVORITES_PER_GROUP)
  @javax.annotation.Nullable
  private Integer maxFavoritesPerGroup;

  public FavoriteGroupList() {
  }

  public FavoriteGroupList favoriteGroups(@javax.annotation.Nonnull List<FavoriteGroupSummary> favoriteGroups) {
    this.favoriteGroups = favoriteGroups;
    return this;
  }

  public FavoriteGroupList addFavoriteGroupsItem(FavoriteGroupSummary favoriteGroupsItem) {
    if (this.favoriteGroups == null) {
      this.favoriteGroups = new ArrayList<>();
    }
    this.favoriteGroups.add(favoriteGroupsItem);
    return this;
  }

  /**
   * Get favoriteGroups
   * @return favoriteGroups
   */
  @javax.annotation.Nonnull
  public List<FavoriteGroupSummary> getFavoriteGroups() {
    return favoriteGroups;
  }

  public void setFavoriteGroups(@javax.annotation.Nonnull List<FavoriteGroupSummary> favoriteGroups) {
    this.favoriteGroups = favoriteGroups;
  }


  public FavoriteGroupList maxFavoriteGroups(@javax.annotation.Nullable Integer maxFavoriteGroups) {
    this.maxFavoriteGroups = maxFavoriteGroups;
    return this;
  }

  /**
   * Only returned when the owner is the currently authenticated user.
   * minimum: 0
   * @return maxFavoriteGroups
   */
  @javax.annotation.Nullable
  public Integer getMaxFavoriteGroups() {
    return maxFavoriteGroups;
  }

  public void setMaxFavoriteGroups(@javax.annotation.Nullable Integer maxFavoriteGroups) {
    this.maxFavoriteGroups = maxFavoriteGroups;
  }


  public FavoriteGroupList maxFavoritesPerGroup(@javax.annotation.Nullable Integer maxFavoritesPerGroup) {
    this.maxFavoritesPerGroup = maxFavoritesPerGroup;
    return this;
  }

  /**
   * Only returned when the owner is the currently authenticated user.
   * minimum: 0
   * @return maxFavoritesPerGroup
   */
  @javax.annotation.Nullable
  public Integer getMaxFavoritesPerGroup() {
    return maxFavoritesPerGroup;
  }

  public void setMaxFavoritesPerGroup(@javax.annotation.Nullable Integer maxFavoritesPerGroup) {
    this.maxFavoritesPerGroup = maxFavoritesPerGroup;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    FavoriteGroupList favoriteGroupList = (FavoriteGroupList) o;
    return Objects.equals(this.favoriteGroups, favoriteGroupList.favoriteGroups) &&
        Objects.equals(this.maxFavoriteGroups, favoriteGroupList.maxFavoriteGroups) &&
        Objects.equals(this.maxFavoritesPerGroup, favoriteGroupList.maxFavoritesPerGroup);
  }

  @Override
  public int hashCode() {
    return Objects.hash(favoriteGroups, maxFavoriteGroups, maxFavoritesPerGroup);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class FavoriteGroupList {\n");
    sb.append("    favoriteGroups: ").append(toIndentedString(favoriteGroups)).append("\n");
    sb.append("    maxFavoriteGroups: ").append(toIndentedString(maxFavoriteGroups)).append("\n");
    sb.append("    maxFavoritesPerGroup: ").append(toIndentedString(maxFavoritesPerGroup)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    return o == null ? "null" : o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("favoriteGroups", "maxFavoriteGroups", "maxFavoritesPerGroup"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(Arrays.asList("favoriteGroups"));
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to FavoriteGroupList
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!FavoriteGroupList.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in FavoriteGroupList is not found in the empty JSON string", FavoriteGroupList.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!FavoriteGroupList.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` in the JSON string is not defined in the `FavoriteGroupList` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : FavoriteGroupList.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("favoriteGroups") != null) {
        if (!jsonObj.get("favoriteGroups").isJsonArray()) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `favoriteGroups` to be an array in the JSON string but got `%s`", jsonObj.get("favoriteGroups").toString()));
        }
        JsonArray jsonArrayfavoriteGroups = jsonObj.getAsJsonArray("favoriteGroups");
        // validate the required field `favoriteGroups` (array)
        for (int i = 0; i < jsonArrayfavoriteGroups.size(); i++) {
          FavoriteGroupSummary.validateJsonElement(jsonArrayfavoriteGroups.get(i));
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!FavoriteGroupList.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'FavoriteGroupList' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<FavoriteGroupList> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(FavoriteGroupList.class));

       return (TypeAdapter<T>) new TypeAdapter<FavoriteGroupList>() {
           @Override
           public void write(JsonWriter out, FavoriteGroupList value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public FavoriteGroupList read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of FavoriteGroupList given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of FavoriteGroupList
   * @throws IOException if the JSON string is invalid with respect to FavoriteGroupList
   */
  public static FavoriteGroupList fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, FavoriteGroupList.class);
  }

  /**
   * Convert an instance of FavoriteGroupList to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

